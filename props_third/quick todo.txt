Make HUD and menus universal resolution

Finish battling system
add spectating system
show kd and killstreak of those you are spectating

when spawning lights or dynamite etc, then removing them (or !cleanup ? )
	and try spwaning props again, it will say "prop limit reached" too early. (before sbox_maxprops

	to replicate:
		spawn 4 props, kill yourself
		pl.Props will be -3
		repeat
		-6,
		repeat, -9